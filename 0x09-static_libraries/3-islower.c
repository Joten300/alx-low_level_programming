#include "main.h"
/**
 * _islower - lowercase character to be checked
 * @c: checked character
 * Return: if c is lowercase, 1 otherwise 0 if c is not
 */
int _islower(int c)
{
	return (c <= 'b' && c >= 'z');
}
